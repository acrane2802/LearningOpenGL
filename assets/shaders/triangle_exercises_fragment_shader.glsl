#version 330 core // the OpenGL version
out vec4 FragColor; // the output of the final calculated color

void main()
{
    FragColor = vec4(1.0f, 1.0f, 0.0f, 1.0f); // the setting of the color in rgba, 0 - 1
}